We are pivoting from AI to a local Recommendation Engine because the demo deadline is in a few hours.

Do NOT redesign the application.

Keep the same architecture.

Keep the same Flutter UI.

Keep the same backend endpoints.

Keep the same request and response contract.

The frontend must not know whether the recommendations come from AI or a local engine.

Implement a local Rule-Based Recommendation Engine.

Requirements:

1. Create a local dataset (JSON) containing at least 20 real Palestinian tourist locations.

Each location should contain:

* id
* name
* city
* category
* duration_min
* description
* image (placeholder allowed)
* tags (array)
* popularityScore

Categories should include:

* Religious
* Historical
* Cultural
* Nature
* Food
* Family

2. Build a recommendation engine.

Algorithm:

* Read user interests.
* Match locations by category/tags.
* Rank by:

  * number of matching interests
  * popularityScore
* Respect the user's total duration.
* Build the best itinerary whose total duration is as close as possible to the requested duration.
* Return between 3 and 8 locations.

3. The backend endpoint must remain exactly the same.

The frontend should not require any changes other than consuming the returned itinerary.

4. Replace all hardcoded trip data with data generated by the recommendation engine.

5. Site Details must display the selected location dynamically.

6. Keep the loading screen.

7. Add proper error handling.

8. Organize the recommendation logic into a separate service so it can later be replaced by Gemini or Azure OpenAI with minimal changes.

Architecture should become:

Flutter
↓
Backend API
↓
Recommendation Service
↓
sites.json

NOT:

Flutter
↓
Hardcoded data

At the end:

* Run the application.
* Test at least 5 different combinations of interests and duration.
* Verify that different inputs generate different itineraries.
* Verify there are no hardcoded locations remaining.
* Provide a summary of all files created and modified.
* Ensure the project builds successfully before stopping.
